#include <stdio.h>

int main()
{

    int a, b, sum;
    printf("Enter the first number :");
    scanf("%d", &a);
    printf("Enter the second Number :");
    scanf("%d", &b);
    sum = a + b;
    printf(" Sum is : %d\n", sum);

    return 0;
}